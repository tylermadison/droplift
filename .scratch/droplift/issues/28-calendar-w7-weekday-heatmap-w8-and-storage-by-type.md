# 28: Calendar (W7), weekday heatmap (W8), and storage by type (W10)

**What to build:** The dashboard shows a 12-month upload calendar heatmap (count or bytes per day), a throughput heatmap by weekday × hour, and storage by file type with drill-down to extension (PRD W7, W8, W10).

**Blocked by:** 21 (Dashboard shell and Recent uploads (W3))

**Status:** ready-for-agent

- [ ] Calendar heatmap shows 12 months; user can switch count/bytes
- [ ] Weekday × hour heatmap shows throughput
- [ ] Treemap/sunburst shows storage by file type and drills down to extension
- [ ] Light and dark mode; table view for each chart
